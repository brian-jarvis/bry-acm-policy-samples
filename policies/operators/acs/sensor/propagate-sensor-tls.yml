---
apiVersion: v1
data: '{{ (eq "{{hub (index $.ManagedClusterLabels `local-cluster`) hub}}" "true") | skipObject }}{{hub copySecretData "stackrox" "sensor-tls" hub}}'
kind: Secret
metadata:
  labels:
    certificate_key_name: sensor-cert.pem
  name: sensor-tls
  namespace: stackrox
type: Opaque
