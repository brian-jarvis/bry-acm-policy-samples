---
apiVersion: v1
data: '{{ (eq "{{hub (index $.ManagedClusterLabels `local-cluster`) hub}}" "true") | skipObject }}{{hub copySecretData "stackrox" "admission-control-tls" hub}}'
kind: Secret
metadata:
  labels:
    certificate_key_name: admission-control-cert.pem
  name: admission-control-tls
  namespace: stackrox
type: Opaque
