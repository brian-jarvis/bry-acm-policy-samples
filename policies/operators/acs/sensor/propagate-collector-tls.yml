---
apiVersion: v1
data: '{{hub (eq (index $.ManagedClusterLabels "local-cluster") "true") | skipObject hub}}{{hub copySecretData "stackrox" "collector-tls" hub}}'
kind: Secret
metadata:
  labels:
    certificate_key_name: collector-cert.pem
  name: collector-tls
  namespace: stackrox
type: Opaque
